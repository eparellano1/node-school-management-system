{
    "version": 2,
    "buildCommand": "npm run server",
    "installCommand": "npm install",

    "routes": [{ 
        "src": "/(.*)", 
        "dest": "/",
        "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]
    }] 
}